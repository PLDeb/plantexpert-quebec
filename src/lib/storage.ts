// Pont côté navigateur entre le diagnostic (page /diagnostic) et la génération de
// guildes (page /guildes), en attendant la persistance en base de données (Palier 5).
export const DIAGNOSTIC_STORAGE_KEY = "plantexpert:diagnostic";
