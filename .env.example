# Copier ce fichier en .env.local et remplir les valeurs (jamais commité sur GitHub).

# Palier 3 — Agent Sylvie
ANTHROPIC_API_KEY=
# Optionnel — modèle par défaut : claude-sonnet-5
ANTHROPIC_MODEL=

# Palier 5 — Supabase + Prisma
# DATABASE_URL = connexion "pooled" (port 6543, via PgBouncer) — utilisée par l'app
# DIRECT_URL   = connexion directe (port 5432) — utilisée par Prisma Migrate/CLI
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
DATABASE_URL=
DIRECT_URL=
